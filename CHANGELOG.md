# 更新日志

本项目的所有显著变更记录于此。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)。

## [0.16.2] - 2026-08-28

### 修复（用户反馈三项）

- **职责折叠面板点击箭头无法收回**：Accordion 补 `multiple` + `collapsible`——可自由展开/收回、支持多角色同时展开；
- **职责输入框太窄**：撑满面板宽度（此前只占内容宽，右侧大片空白）；
- **多角色职责持久化实测确认**：保存/读回链路验证通过（头条+数据+正文三个同时生效）；此前"只能有一个"是 v0.13.2 之前读取白名单丢失字段的历史数据损失，当前版本已可放心多角色自定义。

## [0.16.1] - 2026-08-28

### 新增（用户反馈两项改进）

- **AI 输出心跳**：全部 AI 请求改为 SSE 流式，增量输出实时推送状态栏（"✍ …"滚动显示最近 120 字）——选题/审稿的长等待不再是"没有变化"，写作阶段也能看到 AI 正在写的内容；心跳监听带去注册（修 StrictMode 双执行导致的字符重复）；
- **角色职责编辑器重构**：六个角色改为折叠面板（点角色名展开），标题显示"已自定义"徽标与计数，明确多个自定义职责可同时生效（此前六个大文本框堆叠，用户误以为只能生效一个）。

### 修复

- 流式改造回归：工具调用消息补回 `role: 'assistant'` 字段（工具循环第二轮 400 错误，端到端实测抓到并已修复）。

## [0.16.0] - 2026-08-28

### 新增（ROADMAP Q2 联 · 编辑部流程，质量核心）

- **编辑部三段式**（设置 → 生成 → 开关启用，默认关 = 旧逐槽独立流程）：
  - **选题**：生成前一次 AI 调用通览全部槽位与信息源（带当日缓存），为每槽分配互不重复的选题角度与字数配额，注入写作提示词（含邻槽分工）；
  - **写作**：沿用 3 并发 worker，逐槽按选题写作；
  - **审稿**：全部完成后主编自检（查重复/断裂/职责不符），意见弹出供用户**一键应用重写**或忽略（不做静默改写）；
- **降级路径**：选题/审稿任一环节失败自动回退旧流程，保证任何情况都能出报；
- **阶段进度展示**：状态栏显示“选题中… / 写作中… / 审稿中…”；
- **快照回退**：编辑部模式生成前自动存快照，“文件 → 还原上次生成前快照”可一键还原；
- **独立模型设置**：选题/审稿可指定模型（留空 = 主模型）。

端到端实测：编辑部模式下 2 页 7 槽全部生成成功，选题阶段约 90s，token 总量与旧流程持平（28726 vs 28126），导出 689KB。

## [0.15.0] - 2026-08-28

### 新增（ROADMAP Q5 量 · 质量度量最小层）

- **Token 用量度量**：主进程 AI 响应的 usage 逐请求累加，e2e 报告输出每期"输入/输出/总计"——为 Q2 编辑部三段式的成本评估提供基线（新样例实测 28126 tokens，旧慢源样例 255379）；
- **e2e 质量检查**：生成完成后自动断言——空槽（FAIL）、字数超限（WARN，上限 +25% 容差）、槽间内容 3-gram 相似度粗检（WARN，>35% 疑似重复）、来源署名一致性（FAIL，逐 DOM 槽位核对挂载状态）；
- **快速测试样例**：新增 `C:\Users\sr291\Desktop\test_data\测试报刊.briefy`（快源：quotes/books.toscrape、example.com），覆盖双页/多角色/控件/无源场景；e2e 实测 7/7 生成、质量零问题、导出 690KB，全程 token 仅为旧慢源样例的 11%。

## [0.14.0] - 2026-08-28

### 新增（ROADMAP Q1 准 · 事实与来源）

- **来源署名上版**：挂载了信息源的槽位，内容尾部自动附"来源：xxx"小字（分隔虚线 + 7.5pt 灰阶，随版式参数与黑白优先生效；编辑器与导出 PDF 一致）；
- **时效锚定**：提示词按工具配置区分——已启用搜索/抓取：强制先用工具获取今日事实再写作，禁止凭记忆编造日期数字；未启用：禁止"今日/最新/刚刚"类时效性陈述，只写背景性内容；
- **失败原因标注**：源抓取失败在内容中如实标注具体原因（超时/HTTP 状态），不再笼统；
- **源抓取当日缓存**：同源多槽只抓一次（跨槽共享、隔日失效），消除重复抓取的耗时与浪费。

端到端实测：测试文档 7/7 生成成功，当前页 4 槽均带正确来源署名，导出 PDF 1017KB。

## [0.13.6] - 2026-08-28

### 修复（生成中永久卡住的真正根因）

- **生成期间内容回写丢失**：槽位内容写回（done）会触发溢出测量与摊平重排，槽位可能被挪到另一页；而生成任务的写回用**开始时的旧页 ID** 查找，找不到槽位 → 内容丢失、状态永远停在"生成中"。修复：`updateSlot` 改为全文档按 slotId 定位（与页无关），并清理生成任务签名；
- 端到端实测（与用户卡住场景一致）：修复后 2 页 7 槽全部生成成功，跨页队列连续推进，导出 PDF 981KB。

## [0.13.5] - 2026-08-28

### 新增（端到端验证基建）

- **CDP 自动化验证链路**：dev 模式主进程开启远程调试端口（打包版不启用）；`scripts/e2e.mjs` 可自动加载指定 .briefy → 点击生成 → 轮询全部槽位完成 → 导出 PDF 自动落盘并校验，用于持续回归"生成 + 导出"全链路；
- **dev 自动化钩子**：`dev:read-doc-path`（读任意 .briefy，仅未打包版）、`export:pdf` 支持自动落盘路径（跳过系统对话框，仅 dev）、URL `?autodoc=` 自动加载文档、`window.__briefyGetDoc` 只读探针；
- 首轮端到端实测通过：测试文档 2 页 7 槽全部生成成功，导出 PDF 965KB。

## [0.13.4] - 2026-08-28

### 修复（生成卡住 / 跨页队列停摆）

- **信息源抓取/搜索无超时**：tavilySearch 与网页抓取此前无超时，一个挂住的源就永久卡死该生成任务（IPC 不返回），并发 worker 被逐个拖死——表现为"第一页完成了第二页不开始"。现已加 20s 超时：挂住的源跳过并在内容中如实标注；
- **单槽看门狗**：渲染端每槽 3 分钟兜底超时——超时即中止该任务（调用主进程取消）并报错上屏，worker 立即继续下一任务，个别慢/卡任务不再拖住全局队列。

## [0.13.3] - 2026-08-28

### 修复

- **终止不彻底**：此前点终止只 abort 在途任务，队列里未开始的任务仍会被继续启动。新增全局取消标志：终止后 worker 不再从队列取新任务，在途任务立即中止并复位为空槽；
- **碎片空白页**：摊平重排是顺序贪心，一个大槽位放不下时会堵住同列后续小槽位一起去新页，留下大片空白。分页改为**列内回填**（packSlots）：放不下的槽位去新页但不占列尾，后续更小的槽位仍可装入本页剩余空间（报纸装填语义：小条目先排，大文章跨页）；已加回归断言。

## [0.13.2] - 2026-08-28

### 修复（设置持久化丢失）

- **新设置字段重启即丢**：settings.json 读取侧的字段白名单未包含 P6 新增的 layout（版式参数/黑白优先/页眉页脚）/ stylePrompt（风格提示词）/ roleDuties（角色自定义），保存后读取被丢弃导致重启全部复位。已在读取侧透传并做形状校验（防脏数据但不丢配置）。

## [0.13.1] - 2026-08-28

### 改进

- **报头文字可自定义**：版式设置勾选报头后可填报头文字，留空则用文档标题。

## [0.13.0] - 2026-08-28

### 新增（P6 第二批）

- **页眉页脚（P6c）**：版式设置可选报头（文档标题居左）/ 日期（居右）/ 页码（页脚居中：第 X 页 · 共 N 页）；绘制在页边距区，不占内容高、不影响分页计算；编辑器与导出 PDF 一致生效；
- **控件扩展（P6d）**：新增配图（:::image，URL 直链+图注）、二维码（:::qrcode，qrserver 生成）、本期看点（:::toc，目录式导读）三个控件，注册表/解析/渲染/AI 提示段/参数表单五端同步；CSP img-src 放开 https:。

### 修复

- **设置页信息源编辑器截断**：改为两行布局（名称+网址+删除一行，说明占满一行），不再溢出对话框。

## [0.12.0] - 2026-08-28

### 新增（P6 增强能力包 · 第一批）

- **设置页分区导航改造**：单平铺升级为分区导航（AI 服务 / 版式 / 生成 / 实验性 / 关于），支撑后续增强能力堆叠；
- **版式参数（P6a）**：页边距（10–25mm）/ 栏间距（4–12mm）/ 正文字体 / 字号（8–14pt）/ 行距（1.2–2.0）/ 主题色（色块 + 自定义 hex）——全部可选，留空即默认，缺省体验与旧版完全一致；参数实时影响流式排布、自动分页与渲染；
- **黑白优先（P6a）**：灰阶渲染开关，兼容黑白打印机（导出 PDF 同样灰阶）；
- **全局风格提示词（P6b）**：设置“本报调性”，注入每次生成；
- **角色职责自定义（P6b）**：可覆盖六种槽位角色的默认职责描述。

## [0.11.0] - 2026-08-28

### 新增

- **终止生成**：生成中工具栏"生成"变为"终止"，点击即中止全部任务（主进程 AbortController）；
- **单槽生成**：属性面板新增"生成此槽位"，只重写选中的一格，不用全量重跑；
- **失败自动重试**：单槽生成失败自动重试 1 次，仍失败报错上屏；每步 AI 请求 90s 超时，不再无限等待。

### 改进（版面密度与 AI 篇幅意识）

- **摊平重排**：溢出腾挪/加槽/改宽后全文档重新贪心装满，内容总量装得下 N 页就仍排 N 页——不再出现"2 页设计变 4 页且页页大片空白"；
- **AI 字数硬上限**：提示词按槽位容量换算明确字数上限（约 4.5 字/mm），并告知超出会被裁切，从源头减少超长内容；
- **表格嗅探**：纯文字槽返回 Markdown 表格（≥3 行 `|`）时也自动按真表格渲染，AI 不按 kind 约定也不再出现竖线原文。

## [0.10.2] - 2026-08-28

### 修复（内容突破 A4 / PDF 导出失效）

- **A4 物理硬限制**：纸面容器固定 `height: 297mm + overflow: hidden`——无论内容多长（如超长表格），渲染高度永不突破物理纸张，导出/打印必然合规；
- **溢出自动腾挪**：渲染后实测槽位高度，超出预估（estHeight+overflow）时回写并触发自动分页，把放不下的槽位移到下一页（overflow 字段首次接入闭环；测量只在再超出时增大，天然收敛）；
- **导出链路重修**：此前打印窗口独立加载 `?print=1` 但无任何状态传递，实际导出的是空文档（槽位化重构后已完全失效）。现在导出时把当前文档经 IPC 传给打印窗口（带 preload），逐页渲染真实 A4 版式（与编辑器所见一致，控件协议正常渲染），渲染完成后通知主进程 printToPDF（5s 超时兑底）；打印 CSS 每页强制分页。

## [0.10.1] - 2026-08-28

### 修复（删除槽位后页面卡死）

- **双栏渲染死循环**：PageView 分栏状态机在"左栏耗尽 + 右栏有孤立槽位 + 全宽槽位已渲染完"时，循环体不推进任何游标但循环条件恒真 → 无限渲染循环冻结页面。触发路径：导入预设 → 添加槽位溢出到新页 → 删除新页 → 删除第 1 页左栏预设槽位（右半栏成孤立）→ 卡死。修复：双栏区开启条件纳入右栏 + 每轮无游标推进时防御性 break（穷举 64 种左右栏/全宽组合验证必然终止）。

## [0.10.0] - 2026-08-28

### 变更（信息源槽位化——破坏性数据格式调整）

- **信息源成为槽位属性**：`Slot.sourceIds`（全局源引用）改为 `Slot.sources`（内联源副本，含名称/URL/备注），随 `.briefy` 文档与用户预设一起保存——每个槽位自带"我要依据哪些源"，跨文档/分享预设不再依赖机器上的全局配置；
- **常用信息源（收藏夹）**：设置页的信息源列表语义改为"常用信息源"收藏夹；属性面板可一键勾选导入槽位内联副本、也可把槽位上的新源一键收藏回常用库（按名称+URL 去重，自动持久化）；
- **生成链路**：`ai:generate-slot` IPC 直接接收槽位内联源并抓取，不再查全局配置；preload 桥签名同步更新；
- **旧数据迁移**：打开旧 `.briefy` / 旧用户预设时，`sourceIds` 自动从常用源库解析为内联副本（失效引用丢弃）；`parseLayoutDoc` / `loadDoc` / `fromPresetSlots` 全路径覆盖；
- 属性面板信息源区重写：内联源列表（名称+URL+删除）、三行内联添加、常用源导入勾选区、收藏按钮。

### 修复（内置预设默认工具）

- 内置预设槽位此前只带"当前时间"工具，AI 无法联网取事实。现按角色配默认工具：头条/正文/快讯 → 当前时间+联网搜索+网页抓取；数据 → 当前时间+联网搜索；提示框/自定义 → 当前时间。

## [0.9.4] - 2026-08-28

### 修复（槽位增删异常/疑似卡死）

- **paginate 多栏误判**：自动分页改用槽位实际底部（y + 高度）判断溢出。此前用单一游标顺序累加，双栏布局下左右列 y 独立流动，游标被一栏拉高后会**把另一栏放得下的槽位误判为溢出**，搬去新页——套用预设（页面较满）后添加/删除槽位时槽位莫名消失、页签疯长，表现为"应用异常/卡死"；
- **添加/改宽后自动定位**：新增槽位或切换宽度模式触发分页时，自动切换到该槽位实际所在的页并选中，不再"点了没反应"；
- **setState 纯化**：addSlot/setSlotWidth/addPage/removePage 不再在 setState updater 内做分页或生成随机页 id（React StrictMode 双调用会产生不稳定 UUID），合并为单次状态更新；
- Delete 键监听的 effect 依赖收窄为 selection 与 removeSlot，避免每次渲染重挂监听器。

## [0.9.3] - 2026-08-28

### 修复

- **CSP 补 worker-src**：`index.html` 显式声明 `worker-src 'self' blob:`，避免第三方库/工具创建的 blob Worker 被 `script-src` 兜底拦截（控制台报 CSP 违规告警）。

## [0.9.2] - 2026-08-28

### 修复

- **暗色模式白边**：启动时从 `settings.json` 恢复 `nativeTheme.themeSource`（此前只跟随系统，暗色应用在亮色系统上会带亮色边框/白底窗口），窗口底色按主题设置（`backgroundColor`），切换主题时同步刷新所有窗口底色；
- 槽位悬停边框改为按四边 longhand 设置，暗色下悬停对比度更柔和；槽位底色改用 `colorNeutralBackground2`，与纸面区分更自然；
- 槽位角标显示角色中文名（头条/正文/数据/快讯/提示/自定义）。

## [0.9.1] - 2026-08-28

### 新增

- **悬停提示全覆盖**：工具栏全部按钮（文件/添加槽位/预设/生成/主题/设置）均带引导性 Tooltip——如"添加槽位：选角色即可，AI 按角色分工写作，高度随内容自适应"，新用户无需文档即可上手。

### 改进（暗色模式深度适配）

- **全局 CSS 变量化**：main.css 硬编码色全部替换为 Fluent 主题 CSS 变量（`--colorNeutral*`/`--colorBrand*`），表格/小标题/首字下沉/统计卡/引用块/信息框/时间线在暗色下自动配色；
- 清理 Block 时代遗留死代码样式（a4-sheet/handle/marquee/delete-btn 等无引用类）；
- A4 纸面保持"纸张"语义（亮底黑字），暗色下界面其余部分自动适配。

## [0.9.0] - 2026-08-28

### 新增（信息源系统——个人读物的灵魂）

- **信息源（Sources）**：设置页可增删信息源（名称 + URL + 给 AI 的说明），如"C++ 安全周报"、"GitHub Trending"、"番剧放送表"、MSN 等媒体页——用户自配来源，AI 不再凭记忆编造；
- **槽位挂载信息源**：属性面板勾选该槽位使用哪些源，生成时主进程自动抓取源内容（复用网页抓取+LangChain 切分），注入提示词作为事实依据；单个源抓取失败跳过不阻塞；
- **dev:export-state**：开发模式 IPC，把真实运行状态导出为文件供测试与 AI 助手验证（问题5 的可验证性基建）。

### 改进

- **双栏真实并排**（问题1）：PageView 重构为列分组渲染——左右半栏槽位真实并排，宽度模式产生实际版面效果；全宽槽位照常纵贯；
- **AI 表现力提示词升级**（问题2）：斜体支持、数字优先于形容词、长短句节奏、结论前置、小标题短而有力、信息源事实查证要求。

### 修复

- **属性面板超高无法滚动**（问题6）：面板加 overflow-y，控件手风琴展开后可正常滚动收起；
- 同一提交中 SettingsDialog 历史遗留的孤立 CR 字节清理。

## [0.8.2] - 2026-08-28

### 新增（工程化：测试体系与文档）

- **测试体系**（tsx 直连 TS 源文件，测试与实现零漂移）：
  - `test:parse`：控件协议解析断言（混合内容/未知控件回落/注册表完整性/timeline 多事件）；
  - `test:slots`：槽位版式规则断言（流式排布顺延与分列/自动分页/`.briefy` v1→v2 迁移含废弃工具过滤/非法版本拒绝）；
  - `npm test` 一键全部；
- **`build` 内嵌 IPC 桥校验**：断桥直接构建失败，杜绝"调用了未注册 channel"类问题静默上线；
- **README 全面重写**：功能总览表、开发指南（含国内镜像与 Electron 二进制补齐）、项目结构、架构文档索引、测试说明。

## [0.8.1] - 2026-08-28

### 修复

- **生成报错**：v0.8.0 重构时 preload 桥函数 `generateBlock` 未同步改名（主进程已换 `ai:generate-slot` 并新增 role 参数），导致点击"生成"必然报桥函数不存在；已同步并静态核对全部 13 个 channel；
- **另存为预设无响应**：`window.prompt()` 在 Electron 中不受支持（原生抛错），另存为/重命名预设的输入改为 Fluent Dialog（InputDialog 组件）；同名预设改为覆盖并提示。

### 工程化

- 新增 `scripts/check-ipc.mjs`：构建后自动校验 preload 全部 IPC channel 在主进程都有注册，断桥即构建失败——防止此类问题再次静默漏网。

## [0.8.0] - 2026-08-28

### 重构（架构 v2 第四阶段：槽位化版式，推翻 Block 体系）

- **Slot 取代 Block**：版面 = 槽位声明表；槽位有角色（headline 头条 / body 正文 / stats 数据 / briefs 快讯 / notice 提示框 / custom 自定义），AI 按角色领任务（ROLE_DEFS 职责单一事实源）；
- **流式排布**：用户不再手绘矩形——"添加槽位"选角色即可，同列槽位自动纵向排布（flowSlots），一键切宽度（全宽/左右半栏/右侧栏）自动重排；
- **自动分页**：槽位累计高度超页时自动搬到新页（paginate）；
- **高度自适应一等公民**：CSS 文档流自然撑开（v0.6.0 的 JS 测量方案随 Block 废弃）；
- **兼容迁移**：旧 v1 `.briefy`（blocks）与用户预设打开时自动迁移为 v2 槽位，写回始终 v2；
- 全部既有能力保留复用：控件协议、区块级工具、语篇大纲注入（升级为角色职责版）、用户自定义预设、PDF 导出。

### 修复

- SettingsDialog 相对路径错误（两层应为三层）；
- tsc 严格模式暴露的 CSS side-effect import 声明缺失（新增 css.d.ts）。

## [0.7.0] - 2026-08-28

### 新增（架构 v2 第五阶段：用户自定义预设）

- **把当前版面存为预设**：预设菜单 → "把当前版面存为预设…" → 命名即存；保存内容为版面结构 + 提示词 + 工具配置，**自动剥离 AI 生成内容**（防隐私泄漏）；
- **我的预设管理**：预设菜单分组展示（内置预设 / 我的预设），我的预设每项带 重命名 / 导出 / 删除 快捷操作；
- **导入导出分享**：导出为 `.briefy-preset` 文件，导入即用——用户可互相分享排版心得；
- 存储：`userData/presets/*.json`，主进程 IPC（list/save/exists/delete/rename/export/import），单个文件损坏不影响其余。

## [0.6.0] - 2026-08-28

### 新增（架构 v2 第三阶段：高度自适应）

- **区块高度自适应**：内容超出区块设定高度时自动延展视觉边界，不再截断——AI 不再被"塞进框"折磨；
- **溢出提示**：延展区域以红色虚线底边 + `+Nmm` 角标标注溢出量，提醒用户调整版面（数据模型的 height 保持不变，版面语义与 .briefy 兼容性不受影响）；
- 实现：`BlockBox` 组件用 ResizeObserver 测量内容实际高度（内容/宽度变化自动重测）。

## [0.5.1] - 2026-08-28

### 新增（架构 v2 第二阶段：控件表单化参与）

- **属性面板控件编辑器**：AI 生成后，区块内容中的控件实例自动列出（手风琴折叠），展开即出现按注册表生成的参数表单——改 label/value/趋势等**不写一行代码**，实时回写区块内容与渲染；
- `widget-edit.ts`：控件实例定位/序列化/回写工具（逐行精确替换）。

## [0.5.0] - 2026-08-27

### 新增（架构 v2 第一阶段：预制控件协议）

- **控件注册表**（`src/shared/widgets.ts`）：stat 统计卡 / quote 引用块 / info 信息框 / timeline 时间线，一个定义同时驱动 AI 提示词、语法解析、渲染与（后续）用户表单；
- **控件语法解析器**（`src/shared/parse.ts`）：`:::控件id{key:"值"}` 标记行 → 节点流（段落/小标题/控件），未知控件安全回落为普通文本；
- **控件渲染组件**（`widgets-render.tsx`）：统计卡（趋势色/箭头）、引用块（竖线+来源）、信息框（三色语气）、时间线（时间轴列表）；
- **AI 提示词接入**：告知 AI 可用控件清单与使用克制原则（每区块 0-3 个）；
- 解析器自测脚本 `scripts/test-parse.mjs`（5 节点样例断言全过）。

> 这是"AI 直出 HTML"讨论的落地形态：预制控件协议——AI 有表现力、用户可表单化参与（P2）、版面不可被破坏。架构详见 `docs/DESIGN-widgets.md`。

## [0.4.0] - 2026-08-27

### 新增

- **区块内容表现力增强**（轻量 Markdown 渲染，零新依赖，`src/renderer/src/utils/markdown.tsx`）：
  - 行内强调：`**加粗**` / `*斜体*` / `` `代码` ``，正文与表格单元格均生效；
  - 小标题：AI 可输出 `## 小标题` 分段，渲染为蓝色下划线小节标题；
  - 首字下沉：纯文字区块首段首字放大下沉（报纸排版经典手法）；
  - 主进程提示词同步告知 AI 这些语法可用，并引导其用加粗强调关键数字、把导语放开头。
- 段落两端对齐（text-align: justify），更贴近报纸版式。

### 修复

- 表格单元格内 `**加粗**` 语法原样显示星号的问题（行内解析未覆盖单元格）。

## [0.3.3] - 2026-08-27

### 新增

- **表格真渲染**：表格类区块的 Markdown 表格解析为真正的 HTML `<table>`（带边框/表头样式），不再是竖线糊成一片的纯文本。

### 修复

- **头条区块报"生成超时"**：模型对复杂提示词可能连续多轮调用工具，原 5 步上限过紧（每轮并行多个 tool_calls 各算一步）。上限放宽至 12 步；即使耗尽也不再报错，而是强制模型基于已有工具结果直接产出正文（降级兜底）。

## [0.3.2] - 2026-08-27

### 新增

- **AI 语篇意识**：生成时注入整份报纸的版面大纲（各区块主题与位置），并标注当前区块的方位与职责——头条镇版、后续区块承接过渡、全文风格统一、相邻区块分工不重复。

### 修复

- 内容形式下拉框无法选择：Fluent Dropdown 误用原生 `<option>`，改为 Fluent `<Option>` 组件。

### 变更

- **移除图片类内容形式**（图文/图片）：无生图服务时仅为占位、无实际渲染，按用户要求移除；仅保留纯文字与表格。旧 `.briefy` 文件中的图类形式打开时自动归一化为纯文字。三套预设模板同步重排（不再含图片区块）。

## [0.3.1] - 2026-08-27

### 修复

- **生成结果异常（工具调用循环失效）**：实测复现发现模型首轮返回 `finish_reason: tool_calls`（正文为空）后，AI SDK 的 maxSteps 自动续轮在部分 OpenAI 兼容端点（如 DeepSeek）上未正确回传工具结果，导致最终只显示空内容。改为**手动实现标准两轮工具循环**（tool_calls → 本地执行 → role:'tool' 回传 → 继续生成），用原生 fetch 直连 `/chat/completions`，对各家兼容端点最稳；经 DeepSeek 实测产出正常正文。

### 变更

- 移除对 AI SDK generateText/maxSteps 的依赖，`@ai-sdk/openai-compatible` 仅保留安装备用。

## [0.3.0] - 2026-08-27

### 新增

- **区块级 AI 工具配置**（PRD"配置 AI 使用的工具"兑现）：
  - 属性面板新增工具勾选组：当前时间 / 联网搜索 / 网页抓取，按区块独立勾选；
  - 主进程按区块传入的工具列表动态组装 tools；
  - 数据模型 `Block.tools` 字段；打开旧版 `.briefy` 文件自动补默认工具。
- **联网搜索 + 网页抓取工具**：
  - Tavily 搜索集成（返回标题/链接/摘要，专为 LLM 优化）；
  - Electron `net.fetch` 抓取网页正文，LangChain `RecursiveCharacterTextSplitter` 切分长文取摘要；
  - 引入 LangChain 检索层（混合模式：生成主链路仍为 Vercel AI SDK）。
- **设置页**新增可选 Tavily Key 字段（不配则搜索工具不可用并给出明确报错）。

## [0.2.1] - 2026-08-27

### 新增

- **排版预设**：内置"每日科技报 / 财经日报 / 个人周报"三套模板（`src/shared/presets.ts`），工具栏"预设"菜单一键套用，即刻获得完整版面与预填提示词，点生成即出内容。

### 改进

- **工具栏整理**：新建/打开/保存/导出 PDF 收进"文件"二级菜单，工具栏从 9 个按钮精简为 6 个（文件/添加内容/预设/生成/暗色/设置）。

## [0.2.0] - 2026-08-27

### 新增

- **设计文件保存/打开**：`保存` 按钮 → 系统保存对话框 → `.briefy` 文件（LayoutSpec JSON，透明可手工编辑）；`打开` 按钮 → 选择 `.briefy` → 结构校验后整体加载；`新建` 按钮清空为单页文档。
- **PDF 导出**：隐藏窗口加载 `?print=1` 打印视图（只含所有页面的纯净版式），Electron `printToPDF` 按 A4 零边距输出，弹出系统保存框。零新增依赖。

## [0.1.0] - 2026-08-27

首个可用版本 🎉

### 新增

- **AI 工具系统**：内置 `getCurrentTime` 工具（AI SDK tool calling + zod schema），AI 需要时间信息时自主调用，最多 5 步工具循环；为后续联网搜索/生图等区块级工具配置打好架构基础。

### 修复

- 暗色模式下 Windows 原生标题栏不变色：保存主题时同步 `nativeTheme.themeSource`，启动时恢复；
- 区块提示词标签与 AI 生成内容重叠：生成完成后隐藏标签。

## [0.0.9] - 2026-08-27

### 修复

- **preload 脚本从未加载（严重）**：主进程引用 `../preload/index.js`，但 electron-vite v5 产物是 `index.mjs`，导致 `window.briefy` 桥不存在、所有 IPC（设置读写/生成）静默失效——这正是"保存无效果"的真正根因。现在按实际存在的产物文件自动探测扩展名。

### 验证记录

- 设置持久化链路实测打通：`%APPDATA%\briefy\settings.json` 正确落盘（apiKey/baseUrl/model/theme 四字段完整）。

## [0.0.8] - 2026-08-27

### 修复

- **设置弹窗"保存/取消无效"**：
  - 保存对象缺 `theme` 字段（v0.0.5 引入的字段未同步）导致 IPC 写入数据不完整；
  - Dialog 改为标准受控模式（`onOpenChange`），取消按钮可靠关闭弹窗；
  - 保存异常时兜底关闭弹窗并输出错误日志，不再卡死界面；
  - 保存成功后即时同步 App 状态，工具栏"生成"按钮立即解锁（无需重启）。

## [0.0.7] - 2026-08-27

### 新增

- **AI 生成流水线（核心功能）**：
  - 主进程 `ai.ts`：Vercel AI SDK + OpenAI 兼容 Provider，按区块提示词+内容形式组装报纸文风提示词；
  - IPC `ai:generate-block`：API Key 只在主进程使用，渲染进程不接触密钥；
  - "生成"按钮并发调度（上限 3 并发），跳过无提示词区块，逐块回填；
  - 区块状态机：empty → generating → done/error，画布内实时显示"生成中…"/错误信息/生成内容。

## [0.0.6] - 2026-08-27

### 修复

- 暗色模式下滚动条不变色：自定义 webkit 滚动条样式（thumb 跟随主题灰阶、圆角、hover 变色），并按主题切换 `color-scheme`，系统原生控件同步适配暗色。

## [0.0.5] - 2026-08-27

### 新增

- **多页管理**：底部 Word 式页签栏，添加/切换/删除页面（至少保留一页），删除当前页自动跳转相邻页；
- **暗色模式**（Fluent 设计要求项）：
  - 主题模型升级为 `webLightTheme` / `webDarkTheme`（更贴近 Win11 应用风格）；
  - 工具栏一键亮/暗切换，偏好持久化到 settings.json，Electron 重启后保留。

### 改进（Fluent 化打磨）

- 配色全面迁移到 Fluent Design Token（`colorNeutralBackground*`、`colorBrandStroke1` 等），消灭硬编码十六进制色值；
- 状态栏从"蓝底白字"改为 Fluent 浅灰细底 + 语义色状态图标（绿=已配置/红=未配置）；
- 属性面板改用 `Field` 规范组件 + token 间距排版，删除按钮改用 Fluent Button（红色语义）；
- A4 纸/区块/缩放手柄/框选虚线全部使用 Griffel token 样式，暗色下自动适配；
- 主题 `teamsLightTheme` → `webLightTheme`。

### 修复

- Griffel 不支持 CSS shorthand（border/borderStyle/borderColor 拆分为四边写法）；
- 页面 Hook 条件调用警告；未加载设置时切主题不再覆盖已保存的 API Key。

## [0.0.4] - 2026-08-27

### 新增

- **编辑器核心（M4）**：
  - `LayoutSpec` 数据层（`src/shared/layout.ts`）：页/区块/提示词/内容形式/状态，坐标单位 mm，未来直接序列化为 `.briefy`；
  - `useLayout` Hook：区块增删改、多页管理、选中态，全部不可变更新；
  - A4 画布 `PageView`："添加内容"进入框选模式后在纸上拖出矩形创建区块；
  - 区块拖动移动 + 8 方向缩放手柄（pointer 事件手写实现，无第三方拖拽库）；
  - Delete 键删除选中区块；
  - 属性面板：提示词编辑（实时同步到区块标签）、内容形式下拉（纯文字/图文/表格/图片）、尺寸 SpinButton；
- 交互修正："添加内容"不再依赖 AI Key 配置（排版设计与生成解耦），只有"生成"需要 Key。

## [0.0.3] - 2026-08-27

### 新增

- **UI 全面迁移到 Fluent UI v9**（@fluentui/react-components + react-icons），对齐 Word/Win11 视觉风格；
- 设置弹窗：AI 服务配置（API Key / Base URL / 模型名），保存按钮在 Key 为空时禁用；
- 主进程 IPC：`settings:get` / `settings:set`，配置持久化到 userData 目录的 `settings.json`，读取时过滤脏字段；
- 预加载桥暴露类型安全的 `window.briefy` API（getSettings/saveSettings）；
- 工具栏联动：未配置 API Key 时"添加内容/生成"禁用、设置按钮显示高亮提醒，状态栏显示 AI 配置状态。

### 修复

- 图标名错误（`FileAddRegular` → `DocumentAddRegular`）导致构建失败。

## [0.0.2] - 2026-08-27

### 新增

- Word 风格主框架：顶部工具栏（新建/打开/保存/添加内容/生成/设置）+ 中央 A4 画布 + 右侧属性面板 + 底部状态栏；
- A4 页面渲染组件（mm→px 换算，1mm ≈ 3.7795px @96dpi）；
- 依赖安装：Electron 44 / React 19 / Vite 7 / electron-vite 5（npmmirror 镜像，含 Electron 二进制镜像下载）。

### 修复

- 无（首个功能版本）。

## [0.0.1] - 2026-08-27

### 新增

- 产品需求文档 `docs/PRD.md`（含技术选型决策记录）；
- AI 协作守则 `AGENTS.md`；
- Electron + React + TypeScript 项目骨架：
  - 主进程 `src/main/index.ts`（窗口创建、外部链接处理、开发/生产加载切换）
  - 预加载脚本 `src/preload/index.ts`
  - 渲染进程 React 入口与占位界面
- 构建配置：`electron.vite.config.ts`、`tsconfig.json`；
- `LICENSE`（MIT）、`README.md`、`.gitignore`。
