# Briefy 项目协作规则（给 AI 助手的工作守则）

> 本文件对所有 AI 编码助手生效。开始任何工作前必须先读完本文件。

## 0. 基本约定

- **交流语言**：与用户对话、注释、文档一律使用简体中文；代码标识符用英文。
- **回应风格**：简洁直接，先结论后细节，不废话。

## 1. 决策必须询问用户（最高优先级）

凡涉及以下决策，**禁止自行拍板**，必须使用提问工具（askQuestions）向用户确认后再动手：

- 技术栈选择（框架、语言、构建工具、UI 库等）
- 引入任何新的第三方依赖或删除现有依赖
- 目录结构、架构模式的重大调整
- 数据格式、接口协议、存储方案的变更
- 版本号变更的幅度（打补丁 / 加功能 / 破坏性升级）
- 是否推送到 GitHub（见第 2 条）
- 其他有多个合理选项、且用户偏好不明的事项

只有"无争议、可逆、纯实现细节"的小事（如局部变量命名、函数拆分方式）可以自行决定。

## 2. 版本管理流程

### 2.1 版本号

- 当前版本：**0.0.0**（初始阶段）
- 版本记录位置：`CHANGELOG.md`（创建后维护），技术栈确定后以 `package.json` 等官方字段为准。
- 遵循语义化版本 [SemVer](https://semver.org/lang/zh-CN/)：
  - `MAJOR` 破坏性变更
  - `MINOR` 新功能
  - `PATCH` 缺陷修复
- **每次完成一轮有效改动并交付后**，主动更新版本号并在 `CHANGELOG.md` 记录本次变更摘要。

### 2.2 发布流程（每次版本更新必走）

1. 更新版本号 + 写 `CHANGELOG.md`；
2. 用提问工具询问用户："是否推送到 GitHub？"
3. 用户确认后才执行 `git push`；未经确认不得推送。
4. 若尚未初始化 Git 仓库或未配置远程仓库，直接告知用户需要什么，不要自行变通。

## 3. 工具与环境（国内网络环境）

- 所需的命令行工具、运行时、账号权限等，**如缺失要明确告诉用户装什么、去哪装**，不得擅自找替代方案绕过（例如用"手写脚本"代替应有的包管理器行为）。
- 默认假定用户处于中国大陆网络环境：
  - npm/yarn/pnpm → 优先使用 npmmirror 镜像（`https://registry.npmmirror.com`）；
  - pip → 可使用清华 TUNA 或阿里镜像源；
  - 下载二进制大文件优先考虑镜像站；
  - 涉及网络下载的操作若失败，先说明原因并询问用户，而不是反复重试或换源硬试。
- 不安装来历不明的东西；能复用系统已有工具就不重复安装。

## 4. 工程原则

- **不重复造轮子**：能用标准库、成熟主流库解决的需求，不自研；选库前先向用户展示候选方案让其确认。
- 不过度设计：当前是什么规模就写什么规模的代码，YAGNI。
- 改动保持最小聚焦，不顺手重构无关代码。

## 5. 任务执行方式

- **强制使用 todo 列表**跟踪多步骤任务，每个 todo 项完成后立即标记，让用户随时看到项目进度。
- 复杂任务开工前先给出简短计划，再逐项执行。
- 每完成一个阶段性目标，在回复里汇报当前进度（已完成 x/n）。

## 6. 当前进度

（……历史清单略，最新如下……）
- [x] 建立项目规则（本文件）
- [x] 确定产品定位与技术栈（Electron + React + TS + AI SDK，详见 docs/PRD.md）
- [x] 初始化项目骨架（v0.0.1）
- [x] Word 风格主框架（v0.0.2）
- [x] 设置页：AI Key 配置（v0.0.3，Fluent UI v9）
- [x] 编辑器：A4 版面与内容块增删移动（v0.0.4，框选/拖动/缩放/Delete）
- [x] 多页管理与排版预设（多页+暗色 v0.0.5；三套模板预设 v0.2.1；用户自定义预设 v0.7.0）
- [x] AI 生成流水线（v0.0.7 代码就绪 → v0.1.0 真实验证通过，DeepSeek 实测；v0.3.0 区块级工具配置 + 搜索/抓取工具；v0.5.x 预制控件协议；v0.6.0 高度自适应）
- [x] 设计文件保存/打开（v0.2.0，.briefy JSON + 系统对话框）
- [x] PDF 导出（v0.2.0，printToPDF 零依赖方案）
- [x] 首个可用版本发布（v0.1.0，2026-08-27）
- [x] 槽位化版式（v0.8.0，Slot 取代 Block：角色职责/流式排布/自动分页，详见 docs/DESIGN-slots.md）
- [x] 暗色模式白边修复 + UI 优化（v0.9.2，启动恢复 nativeTheme.themeSource + 窗口底色同步；槽位悬停/角色中文名）
- [x] 槽位增删异常修复（v0.9.4，paginate 多栏溢出误判 + 分页后自动定位新槽位）
- [x] 信息源槽位化（v0.10.0，Slot.sources 内联副本 + 常用信息源收藏夹；内置预设按角色配默认工具）
- [x] 双栏渲染死循环修复（v0.10.1，右栏孤立槽位场景）
- [x] A4 物理硬限制 + 溢出自动腾挪 + PDF 导出链路重修（v0.10.2）
- [x] 终止生成/单槽生成/失败重试 + 摊平重排 + AI 字数硬上限 + 表格嗅探（v0.11.0）
- [x] P6a 设置页分区导航 + 版式参数 + 黑白优先；P6b 全局风格提示词 + 角色职责自定义（v0.12.0，下一批 P6c 排版升级 / P6d 控件扩展）
- [x] P6c 页眉页脚 + P6d 三新控件（配图/二维码/本期看点）+ 信息源编辑器布局修复（v0.13.0；待做：混排栏 L1、天气卡）
- [x] 端到端自动化验证链路（v0.13.5，scripts/e2e.mjs：加载→生成→导出全链路实测通过）
- [x] ROADMAP Q1 准（v0.14.0：来源署名/时效锚定/失败原因/源缓存；下一批 Q5 最小层 → Q2 编辑部三段式）
- [x] ROADMAP Q5 量最小层（v0.15.0：e2e 质量断言 + token 度量 + 快速测试样例；下一批 Q2 编辑部三段式）
- [x] ROADMAP Q2 联 · 编辑部三段式（v0.16.0：选题/审稿 IPC + 降级 + 阶段进度 + 快照还原 + 独立模型；下一批 Q3 富）
- [x] ROADMAP Q3 富（v0.18.0：Tavily 图搜配图闭环 / :::chart SVG 图表 / 多栏正文流；下一批 Q4 美）
- [x] ROADMAP Q4 美 收官（v0.19.0：头条三段式引题/主标/副题 + AI 工作台浮动面板可拖动持久化；混排栏 L1 经确认已由宽度模式覆盖，不另开发；Q1-Q5 全部完成）
- [x] 长度工程 + 审稿自动执行 + 质量报告卡（v0.20.0，退稿重写/enforceLength/质量卡）
- [x] 长度工程策略修正（v0.21.0：超长内容不砍内容，渲染层自适应缩字号适配槽位（下限 70%），退稿重写保留；enforceLength 弃用保留）
- [x] 长度协调（v0.22.0：可接受区间 80%~115%；偏差小双向字号微调（少→增字号上限 125%/多→缩字号下限 70%，到下限仍超放宽槽位兜底）；偏差大退稿重写双方向（超长压缩/太短扩写））
- [x] 手动布局模式（v0.23.0：自动排布⇄手动布局切换；手动下槽位绝对定位可拖拽移动/拖角缩放/拖出底部跨页掉落；字数协调照常生效；添加槽位菜单加自定义角色）
- [x] 打磨批（v0.24.0：拖拽跨页双向（页顶回上一页/页底掉下一页）+ 边界松手反馈提示条；生成中槽位呼吸/扫光/跳点动画；页签前后移排序 + 槽位数显示）
- [x] 体积折算口径（v0.24.1：estimateQuota——正文文字 + 控件按占版面积折算等效字数（高度mm×4.5），统计卡15/时间线8每事件/配图32/二维码26/目录10每条目/图表40+3每数据点 mm；报告卡改「体积/上限」；纯控件槽位不退稿）
- [x] 控件自适应缩放（v0.24.2：控件节点包 zoom: var(--briefy-fit)，图片/图表/引用框等占版体积随字号缩放系数联动；估算表只是退稿初筛，渲染层实测收敛为最终兜底；探针 probe-zoom.mjs）
- [x] 估算降级为粗筛（v0.24.3：退稿仅限极端偏差（超 2 倍/不足 40%），普通偏差交渲染层字号自适应；报告卡改实测判定（SlotBox 回写字号比例/溢出，带去重防测量循环）；token 降 30%）
- [x] 文件参考源（v0.25.0：InfoSource 加 kind:file + path，支持 txt/md/csv/json/log/pdf/docx（新增 pdf-parse/mammoth，npmmirror 安装，pdf-parse 需 @ts-ignore 引内部模块）；AI 经 readSource 工具按需分块读取（4000 字/块）每文件限 3 次，不预注入；pick-file IPC + 设置页/属性面板入口；实测 AI 精确引用文件事实）
- [ ] 订阅功能（v0.26.0 已实现：订阅管理 Dialog（模板固化无 Key/推送新一期/重新出刊指定期/归档目录）+ 分层记忆（最近 3 期+滚动总览）+ 连载线直通（prompt 含继续/连载注入上期全文）+ 强化审查 2 轮 + PDF 归档；生成/选题/审稿 IPC 支持 overrides 内存态覆盖；settingsRef/docRef 消除出刊闭包陷阱；单测 test:subscription；待用户实测出刊闭环）

## 7. P6 增强能力包计划（v0.12 起，已与用户对齐）

原则：全部做成**设置可选项**，默认值 = 现有稳定体验；实验功能集中在"实验性"分区，稳定即转正并移除开关。

- 分级共识：排版自由化分 L1（混排栏/跨栏）→ L2（版式参数）→ L3（非矩形/环绕，观望）
- **P6a 版式基座**：设置页 Dialog 分区导航（常规/AI 服务/版式/生成/实验性/关于）+ 版式参数（页边距/栏距/字体/字号/行距/主题色/**黑白优先**——兼容黑白打印机）+ 实验区框架
- **P6b 生成增强**：全局风格提示词（本报调性）+ 角色职责自定义（覆盖 ROLE_DEFS duty）
- **P6c 排版升级**：混排栏/跨栏（L1）+ 页眉页脚（报头/日期/页码）+ 跨页续读标注（"接第 X 页"）
- **P6d 控件扩展**：图片（URL）/目录/天气卡/二维码占位等，按需逐个加
- **P6e**（观望）：L3 自由形态

## 8. 备忘

- 用户偏好图文并茂、易于分享的表达形式（来自既往记忆）。
- 本文件随项目演进持续更新，每次重要约定变化都要同步到这里。
