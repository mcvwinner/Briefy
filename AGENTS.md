# Briefy 项目协作规则（给 AI 助手的工作守则）

> 本文件对所有 AI 编码助手生效。开始任何工作前必须先读完本文件。

## 0. 基本约定

- **交流语言**：与用户对话、注释、文档一律使用简体中文；代码标识符用英文。
- **回应风格**：简洁直接，先结论后细节，不废话。

## 1. 决策必须询问用户（最高优先级）

凡涉及以下决策，**禁止自行拍板**，必须使用提问工具（askQuestions）向用户确认后再动手：

- 技术栈选择（框架、语言、构建工具、UI 库等）
- 引入任何新的第三方依赖或删除现有依赖
- 目录结构、架构模式的重大调整
- 数据格式、接口协议、存储方案的变更
- 版本号变更的幅度（打补丁 / 加功能 / 破坏性升级）
- 是否推送到 GitHub（见第 2 条）
- 其他有多个合理选项、且用户偏好不明的事项

只有"无争议、可逆、纯实现细节"的小事（如局部变量命名、函数拆分方式）可以自行决定。

## 2. 版本管理流程

### 2.1 版本号

- 当前版本：**0.0.0**（初始阶段）
- 版本记录位置：`CHANGELOG.md`（创建后维护），技术栈确定后以 `package.json` 等官方字段为准。
- 遵循语义化版本 [SemVer](https://semver.org/lang/zh-CN/)：
  - `MAJOR` 破坏性变更
  - `MINOR` 新功能
  - `PATCH` 缺陷修复
- **每次完成一轮有效改动并交付后**，主动更新版本号并在 `CHANGELOG.md` 记录本次变更摘要。

### 2.2 发布流程（每次版本更新必走）

1. 更新版本号 + 写 `CHANGELOG.md`；
2. 用提问工具询问用户："是否推送到 GitHub？"
3. 用户确认后才执行 `git push`；未经确认不得推送。
4. 若尚未初始化 Git 仓库或未配置远程仓库，直接告知用户需要什么，不要自行变通。

## 3. 工具与环境（国内网络环境）

- 所需的命令行工具、运行时、账号权限等，**如缺失要明确告诉用户装什么、去哪装**，不得擅自找替代方案绕过（例如用"手写脚本"代替应有的包管理器行为）。
- 默认假定用户处于中国大陆网络环境：
  - npm/yarn/pnpm → 优先使用 npmmirror 镜像（`https://registry.npmmirror.com`）；
  - pip → 可使用清华 TUNA 或阿里镜像源；
  - 下载二进制大文件优先考虑镜像站；
  - 涉及网络下载的操作若失败，先说明原因并询问用户，而不是反复重试或换源硬试。
- 不安装来历不明的东西；能复用系统已有工具就不重复安装。

## 4. 工程原则

- **不重复造轮子**：能用标准库、成熟主流库解决的需求，不自研；选库前先向用户展示候选方案让其确认。
- 不过度设计：当前是什么规模就写什么规模的代码，YAGNI。
- 改动保持最小聚焦，不顺手重构无关代码。

## 5. 任务执行方式

- **强制使用 todo 列表**跟踪多步骤任务，每个 todo 项完成后立即标记，让用户随时看到项目进度。
- 复杂任务开工前先给出简短计划，再逐项执行。
- 每完成一个阶段性目标，在回复里汇报当前进度（已完成 x/n）。

## 6. 当前进度

（……历史清单略，最新如下……）
- [x] 建立项目规则（本文件）
- [x] 确定产品定位与技术栈（Electron + React + TS + AI SDK，详见 docs/PRD.md）
- [x] 初始化项目骨架（v0.0.1）
- [x] Word 风格主框架（v0.0.2）
- [x] 设置页：AI Key 配置（v0.0.3，Fluent UI v9）
- [x] 编辑器：A4 版面与内容块增删移动（v0.0.4，框选/拖动/缩放/Delete）
- [x] 多页管理与排版预设（多页+暗色 v0.0.5；三套模板预设 v0.2.1；用户自定义预设 v0.7.0）
- [x] AI 生成流水线（v0.0.7 代码就绪 → v0.1.0 真实验证通过，DeepSeek 实测；v0.3.0 区块级工具配置 + 搜索/抓取工具；v0.5.x 预制控件协议；v0.6.0 高度自适应）
- [x] 设计文件保存/打开（v0.2.0，.briefy JSON + 系统对话框）
- [x] PDF 导出（v0.2.0，printToPDF 零依赖方案）
- [x] 首个可用版本发布（v0.1.0，2026-08-27）
- [x] 槽位化版式（v0.8.0，Slot 取代 Block：角色职责/流式排布/自动分页，详见 docs/DESIGN-slots.md）
- [x] 暗色模式白边修复 + UI 优化（v0.9.2，启动恢复 nativeTheme.themeSource + 窗口底色同步；槽位悬停/角色中文名）
- [x] 槽位增删异常修复（v0.9.4，paginate 多栏溢出误判 + 分页后自动定位新槽位）
- [x] 信息源槽位化（v0.10.0，Slot.sources 内联副本 + 常用信息源收藏夹；内置预设按角色配默认工具）
- [x] 双栏渲染死循环修复（v0.10.1，右栏孤立槽位场景）
- [x] A4 物理硬限制 + 溢出自动腾挪 + PDF 导出链路重修（v0.10.2）
- [x] 终止生成/单槽生成/失败重试 + 摊平重排 + AI 字数硬上限 + 表格嗅探（v0.11.0）
- [x] P6a 设置页分区导航 + 版式参数 + 黑白优先；P6b 全局风格提示词 + 角色职责自定义（v0.12.0，下一批 P6c 排版升级 / P6d 控件扩展）
- [x] P6c 页眉页脚 + P6d 三新控件（配图/二维码/本期看点）+ 信息源编辑器布局修复（v0.13.0；待做：混排栏 L1、天气卡）
- [x] 端到端自动化验证链路（v0.13.5，scripts/e2e.mjs：加载→生成→导出全链路实测通过）
- [x] ROADMAP Q1 准（v0.14.0：来源署名/时效锚定/失败原因/源缓存；下一批 Q5 最小层 → Q2 编辑部三段式）
- [x] ROADMAP Q5 量最小层（v0.15.0：e2e 质量断言 + token 度量 + 快速测试样例；下一批 Q2 编辑部三段式）
- [x] ROADMAP Q2 联 · 编辑部三段式（v0.16.0：选题/审稿 IPC + 降级 + 阶段进度 + 快照还原 + 独立模型；下一批 Q3 富）
- [x] ROADMAP Q3 富（v0.18.0：Tavily 图搜配图闭环 / :::chart SVG 图表 / 多栏正文流；下一批 Q4 美）
- [x] ROADMAP Q4 美 收官（v0.19.0：头条三段式引题/主标/副题 + AI 工作台浮动面板可拖动持久化；混排栏 L1 经确认已由宽度模式覆盖，不另开发；Q1-Q5 全部完成）
- [x] 长度工程 + 审稿自动执行 + 质量报告卡（v0.20.0，退稿重写/enforceLength/质量卡）
- [x] 长度工程策略修正（v0.21.0：超长内容不砍内容，渲染层自适应缩字号适配槽位（下限 70%），退稿重写保留；enforceLength 弃用保留）
- [x] 长度协调（v0.22.0：可接受区间 80%~115%；偏差小双向字号微调（少→增字号上限 125%/多→缩字号下限 70%，到下限仍超放宽槽位兜底）；偏差大退稿重写双方向（超长压缩/太短扩写））
- [x] 手动布局模式（v0.23.0：自动排布⇄手动布局切换；手动下槽位绝对定位可拖拽移动/拖角缩放/拖出底部跨页掉落；字数协调照常生效；添加槽位菜单加自定义角色）
- [x] 打磨批（v0.24.0：拖拽跨页双向（页顶回上一页/页底掉下一页）+ 边界松手反馈提示条；生成中槽位呼吸/扫光/跳点动画；页签前后移排序 + 槽位数显示）
- [x] 体积折算口径（v0.24.1：estimateQuota——正文文字 + 控件按占版面积折算等效字数（高度mm×4.5），统计卡15/时间线8每事件/配图32/二维码26/目录10每条目/图表40+3每数据点 mm；报告卡改「体积/上限」；纯控件槽位不退稿）
- [x] 控件自适应缩放（v0.24.2：控件节点包 zoom: var(--briefy-fit)，图片/图表/引用框等占版体积随字号缩放系数联动；估算表只是退稿初筛，渲染层实测收敛为最终兜底；探针 probe-zoom.mjs）
- [x] 估算降级为粗筛（v0.24.3：退稿仅限极端偏差（超 2 倍/不足 40%），普通偏差交渲染层字号自适应；报告卡改实测判定（SlotBox 回写字号比例/溢出，带去重防测量循环）；token 降 30%）
- [x] 文件参考源（v0.25.0：InfoSource 加 kind:file + path，支持 txt/md/csv/json/log/pdf/docx（新增 pdf-parse/mammoth，npmmirror 安装，pdf-parse 需 @ts-ignore 引内部模块）；AI 经 readSource 工具按需分块读取（4000 字/块）每文件限 3 次，不预注入；pick-file IPC + 设置页/属性面板入口；实测 AI 精确引用文件事实）
- [x] 订阅功能（v0.26.0 已实现并实测通过：订阅管理 Dialog（模板固化无 Key/推送新一期/重新出刊指定期/归档目录）+ 分层记忆（最近 3 期+滚动总览）+ 连载线直通（prompt 含继续/连载注入上期全文）+ 强化审查 2 轮 + PDF 归档；生成/选题/审稿 IPC 支持 overrides 内存态覆盖；settingsRef/docRef 消除出刊闭包陷阱；单测 test:subscription）
- [x] AI 提炼订阅记忆（v0.27.0：出刊后 summarizeIssue 档案员调用提炼结构化摘要（headline+每栏要点保关键事实），失败降级截断摘要；摘要模型用审稿模型）
- [x] 打磨批（v0.28.0：槽位边框按角色线型多样化（头条双线/数据虚线/快讯点线…）+ 徽章按需显形；手动布局按钮移位；属性面板 hint 悬停引导；README 全面重写；下一批：手动布局参考线/键盘微调 → 自由槽位 → 关联槽位机制）
- [x] 批二（v0.29.0：手动布局体验（对齐参考线+5mm 网格吸附+拖拽数值提示+键盘微调 1/5mm）；自由槽位（free 角色全工具/审查豁免/自然流）；关联槽位（child 子槽父先子后注入父内容全文 + continuation 接续组合并一次调用 ═══PART═══ 拆分回填，存在关联时跳过编辑部三段式）；修复：槽位去背景色/打印隐藏徽章/手动徽章内嵌）
- [x] 关联槽位超时修复（v0.29.1：根因=主进程 90s 总时长硬超时砍掉接续组长生成；改空闲超时机制——流式 90s 无输出才断/总上限 600s，非流式 600s；实测每日报刊 8/8 全通；教训：CDP 探针每次操作用独立短连接（connect-eval-close），同 target 多 WS 客户端会挂起；生成链路 [gen] 诊断日志常驻）
- [x] 编辑指令通道（v0.30.0：EditorialPrefs 加 plannerNote/reviewerNote，设置 → 生成 → 编辑部模式区两个文本域；分别注入选题/审稿 system prompt（主编特别指令优先级高于默认规则）；随订阅模板固化；overrides 自动兼容）
- [x] 手动布局出界修复（v0.30.1：根因=手动模式 onOverflow 无限增高槽位长出纸张底边；改为溢出只吸收到页面底边为止（版面锁定），极限后裁剪+质检标记瑕疵；带回写去重防无限循环；教训：手动模式“版面神圣”语义下内容适配必须有钳制终点）
- [x] 版面适配循环实验化（v0.31.0：适配循环读闭包快照导致越适越乱 → slotFitsRef 实时读 + 去打回 + est 单轮钳 ±40% + 列内流式重排 reflowManualPage（列起点锚定/顺序保持）；改为实验性开关（Subscription.experimentalLayoutFit，订阅创建时勾选，默认关）；教训：跨渲染迭代的实测数据必须走 ref 不能走闭包）
- [x] 导出所见即所得（v0.32.0：根因=打印窗口独立渲染 fitScale 重新收敛+窗口尺寸不同+无溢出回写，导出 PDF 与应用内不一致；修复=主窗口收敛后的每槽 fitScale 终值随文档传打印窗口，打印模式锁定 fitScale 禁用本地收敛；export:get-doc 返回 {doc,fits}；教训：打印窗口绝不能重新排版，所见即所得是铁律）
- [x] 导出设置竞态修复（v0.32.1：打印窗口 settings 异步加载，printToPDF 可能在版式装载前截取（全落默认值）；设置快照随导出数据同步传递 + 主题/accentColor 同步 + 装载完成前不渲染；教训：导出链路所有输入必须原子传递，任何异步装载都是竞态源）

- [x] 订阅长期记忆升级（v0.33.0：相关往期 3-gram 检索注入差异化块 + digest 溢出 AI 压缩（失败降级拼接）+ ai:compress-digest IPC + 单测扩展；教训：现有订阅记忆「recent 摘要 + 字面拼接 digest」升级优先做零依赖精准检索而非向量库）

- [x] 导出一致性三连修（v0.33.1：预览与 PDF 仍有字号/裁剪/图表差异；根因一=打印窗口 --briefy-fit 绑本地 fitScale（恒 1），printScale 只锁测量没锁样式，PDF 以 100% 字号重排挤出裁剪；根因二=lazy 图片在隐藏窗口永不加载 → 配图/二维码空白；根因三=renderReady 固定 100ms 竞态，改图片就绪+350ms 收敛窗口+双 rAF；教训：传递"终值"必须连样式一起锁，只锁测量逻辑等于没锁）

- [x] Token 优化·搜索层（v0.34.0：搜索结果当日缓存 + in-flight 合并（3 路并发的同词惊群）；归一化 key 保留分词空格；新增 test:search-cache 6 断言（fetch 计数零网络）；实测输入 421K 中搜索往返占四成，prompt 静态注入瘦身评估过暂不做；教训：多轮工具调用每次都重发全量 messages，省请求次数 > 省 prompt 字数）

- [x] 设置项：来源署名开关 + 实验开关迁移（v0.34.1：LayoutPrefs.showSources 默认开，关后预览/导出均不渲染来源署名；「订阅出刊自动适配版面」自订阅创建对话框迁至设置→实验性（AiSettings.experimentalLayoutFit），设置显式存布尔可覆盖旧订阅遗留字段（教训：布尔开关跨迁移时存 undefined 会让旧值永远赢，必须显式写 false））

- [x] 打印几何修复（v0.34.2：末页底侧截断根因=打印窗口 body UA 默认 8px margin 从未清零——水平挤出纸宽触发 Chrome 打印整体缩放(~0.99×)+垂直每页底推 2mm；修=main.css @page A4 margin0 + @media print body margin0 + JS 双保险；探针 probe-print-geo.mjs 实测 margin=0/纸面(0,0)/sheet 精确 210×297/PDF MediaBox 标准 A4；教训：屏幕 CSS mm 是 96dpi 固定换算，Windows 显示缩放让屏幕纸面"偏小"属正常；Griffel 哈希类名无法用 [class*="语义名"] 选择，探针须用 .print-page > div）

- [x] 增字号填充考虑富媒体 + 字数上限口径修正 + 审稿富媒体感知（v0.34.3：hasRichMedia 嗅探（image/qrcode/chart 控件+Markdown 表格）→ 这类槽位跳过增字号填充只留白（zoom 联动放大图片会糊/图表占版失衡）；字数上限 prompt 告知控件等效成本（widgetQuotaHint 与 estimateQuota 同源折算：图144/码117/图180+14每点/表行25）；estimateQuota 表格行按 5.5mm/行折算修正原字符计数低估 3 倍；审稿 prompt 标注稿件媒体构成并检查图文一致性（describeMedia）；探针 probe-fit-rich.mjs 实测纯文字槽 fit=1.25 vs 图片槽 fit=1；全判断点盘点：缩小/增高以实测驱动天然准、收缩/质检自洽、唯审稿有缺口已补；教训：给 AI 的上限若不告知控件成本，AI 会写满上限再插控件 → 体积必然超）

- [x] 长度协调口径三修（v0.34.4：①误判为空根因=口径分裂——渲染层 parseContent 把无参 ::: 行按段落渲染（内容正常），评估层"多行块容错"却开块吞掉后面全部正文直到 ::: → 估计≈0 误判为空；修=评估层与渲染层严格对齐，仅可解析单行控件按控件折算/剥离，无参行按文字计，永不吞正文；②下限 80%→85%；③quotaRange：上限>600 字的槽位改固定字数冗余 -100/+150（比例冗余在大槽位失真是"超大槽位字数不够"主因），退稿判断与报告卡共用；教训：同一内容在渲染层和评估层必须用同一套解析口径，"容错"逻辑如果两处不一致就是 bug 温床）

- [x] 字数预测密度模型（v0.34.5：根因=恒定 4.5 字/mm 只对半栏宽成立，全宽槽实际 ~9.6 字/mm，容量被低估 2 倍；estimateQuota 重构为行数模型——段字数→每行字数→行数取整→行高，含段尾半行/段距/小标题/槽内边距/首段下沉全部冗余；slotWordCapacity 容量同步密度化，prompt 上限/退稿/报告共用；IPC 加 widthMM；探针 probe-density.mjs 实测全宽误差 0%（旧 62%）/半栏 7%（旧 12%）；教训：multiline fit² 关系——字号放大时行数行高都变，实测还原基准要除 fit²）
- [x] P0 稳定性止血（v0.34.6：修复普通生成遗漏栏宽、任务未完成即提前取消、输入框 Delete 误删槽位；增加未保存标记与替换/退出保护；界面版本改读 package.json；新增编辑安全与生成生命周期回归测试）

## 7. P6 增强能力包计划（v0.12 起，已与用户对齐）

原则：全部做成**设置可选项**，默认值 = 现有稳定体验；实验功能集中在"实验性"分区，稳定即转正并移除开关。

- 分级共识：排版自由化分 L1（混排栏/跨栏）→ L2（版式参数）→ L3（非矩形/环绕，观望）
- **P6a 版式基座**：设置页 Dialog 分区导航（常规/AI 服务/版式/生成/实验性/关于）+ 版式参数（页边距/栏距/字体/字号/行距/主题色/**黑白优先**——兼容黑白打印机）+ 实验区框架
- **P6b 生成增强**：全局风格提示词（本报调性）+ 角色职责自定义（覆盖 ROLE_DEFS duty）
- **P6c 排版升级**：混排栏/跨栏（L1）+ 页眉页脚（报头/日期/页码）+ 跨页续读标注（"接第 X 页"）
- **P6d 控件扩展**：图片（URL）/目录/天气卡/二维码占位等，按需逐个加
- **P6e**（观望）：L3 自由形态

## 8. 备忘

- 用户偏好图文并茂、易于分享的表达形式（来自既往记忆）。
- 本文件随项目演进持续更新，每次重要约定变化都要同步到这里。
